/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export", // Specifies the output format for the build (e.g., "export", "browser")
    // images: {
    //   loader: 'custom',
    //   loaderFile: './my-loader.ts',
    // },
  };
  
  module.exports = nextConfig;
  